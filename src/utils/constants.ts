
/**
 * Email validation regular expression.
 */
export const EMAIL_REGEX = /^([a-zA-Z0-9_\-\.]+)@(([a-zA-Z0-9\-]+\.)+)([a-zA-Z]{2,})$/i

/**
 * Password minimum allowed length.
 */
export const PASSWORD_MIN_LENGTH = 5
