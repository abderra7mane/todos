
/**
 * Email validation regular expression.
 */
export const EMAIL_REGEX = /^([a-zA-Z0-9_\-\.]+)@(([a-zA-Z0-9\-]+\.)+)([a-zA-Z]{2,})$/i

/**
 * Name minimum allowed length.
 */
export const NAME_MIN_LENGTH = 3

/**
 * Password minimum allowed length.
 */
export const PASSWORD_MIN_LENGTH = 5

/**
 * JWT signing and verification secret.
 */
export const JWT_SECRET = 'EJ*TRaZxxdWEZg^@T_euYX&A3Mt2-9Cc4J2#-BLm4vh67kJz'

/**
 * Expiration time for JWT token.
 */
export const JWT_EXPIRATION_TIME = '1 day'

